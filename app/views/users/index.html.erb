<h1>Users</h1>

<p>
  <% @users.each do |user| %>
    <li>
      <strong><%= link_to user.name, user_binges_path(user) %></strong> 
    </li>
  <% end %>
</p>

<%= link_to "New User", new_user_path %> 